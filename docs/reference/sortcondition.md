# Creating a keyed list of conditions to the list of proteins that are present

Create a keyed dictionary, where every unique experimental condition is
the label for a list of every protein that has a value for that
condition.

## Usage

``` r
sortcondition(dataSet)
```

## Arguments

- dataSet:

  The 2d data set of experimental values.

## Value

A list of lists.
